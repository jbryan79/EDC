[build]
  command = "npm install && node src/build.js"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect rules (if needed)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
